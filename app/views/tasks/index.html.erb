<h1>All Tasks/h1>
<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p><%= task.description %></p>
  <%= link_to "Delete", tasks_path,
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>

